{
  "name": "getting-started",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:svelte": "turbo run dev --parallel --filter='./svelte/*'",
    "dev:react": "turbo run dev --parallel --filter='./react/*'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.8.3"
  },
  "workspaces": [
    "svelte/*"
  ]
}
